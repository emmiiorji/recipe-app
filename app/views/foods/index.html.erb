<p style="color: green"><%= notice %></p>

<%= link_to 'Add New Food', new_food_path, class:"btn btn-primary" %>

<div id="foods">
  <% @foods.each do |food| %>
    <%= render food %>
  <% end %>
</div>

